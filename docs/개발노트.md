# project_bidbot 개발노트

> 시작: 2026-04-19
> 단일 노트 원칙 — 분량이 감당 안 될 때만 분리

---

## 1. 프로젝트 요약

구글시트 → 멀티마켓 입찰/판매 자동 동기화. 1차 KREAM(POC), 2차 POIZON/네이버.

- 등록: 시트 행 → 마켓 입찰
- 삭제: 재고 0 / 행 제거 → 마켓 취소
- 리프라이싱: 경쟁최저가 추적 → 자동 조정
- 입찰가: `max(경쟁최저가 - 1000, 하한가)`
- KREAM 재고: 1건만 가능 → 0/비0만 구분

## 2. 구글시트 스키마 (14열)

브랜드 · 포이즌(V) · 크림(V) · 출고처 · 갱신일 · NAME · CODE · SIZE · 수량 · 정상가 · 할인율 · 공급가 · 크림업로드가격 · 포이즌업로드가격

- 공급가 = 정상가 × (1-할인율)
- 크림/포이즌업로드가격 = 프로그램이 역기입
- 원본 엑셀: `C:\Users\hicor\Downloads\초.xlsx`
- 확인 필요: 할인율 포맷(0.3 vs 30%), V 표기(TRUE/O/체크박스), 헤더 행 위치, 하한가 출처

## 3. 배포 전략

1차: **GitHub Actions POC** (private repo)
- Phase A: 로그인만 — 차단/2차인증/캡챠 확인 (**방향성 70% 결정**)
- Phase B: 조회 API
- Phase C: 입찰 1건 등록→즉시 취소 (쓰기 가능성)
- Phase D: 크론 5분 × 24h (지연/차단 실측)

실패 시: 집 미니PC → VPS. 오라클은 제외.

## 4. KREAM_BASE 재활용 맵

원본: `e:\2. hicor\Python\KREAM_BASE` — **참조/발췌만, 원본 수정 금지**

- `Module/Util/auth/로그인Class.py` → bidbot 내 복사 후 개조
- `Module/Util/auth/IP_체크Class.py` → 동일
- `Module/Util/auth/매크로_로그인.py` → 필요 부분만
- `KREAM_RESELLA.py` → 입찰 로직 발췌
- 참고용: `google_spread.py`, `Chrome_기능정리.py`, `공통.py`, `설정값.py`, `마켓프리셋.py`

## 5. 결정 이력

- **신규 레포로 분리** (KREAM_BASE 오염 방지)
- **GitHub Actions POC 우선** (무료 + 크론 + 시크릿)
- **한글 변수/파일명 허용** (기존 컨벤션 유지)
- **docs는 이 파일 하나로 운영** (파일 분할 최소화)

## 6. 결정 대기

- 폴더명 한글 vs 영문
- repo 이름: `project_bidbot` vs `bidbot`
- 브라우저 자동화: playwright / selenium / requests (KREAM_BASE가 뭐 쓰는지 확인 후)

## 7. Phase 실측 결과

### Phase A — KREAM 이메일 로그인 (GitHub Actions)

- **목적**: Actions(Azure IP)에서 KREAM 로그인 API 가 정상 동작하는지 확인
- **구현 위치**: `test/phase_a_kream_login/`
  - `kream_login.py` — 필수헤더 획득 + 이메일 로그인 API 호출
  - `run_phase_a.py` — 실행 엔트리 (토큰 마스킹 출력)
- **원본 참조**: `KREAM_BASE/Module/Function/KREAM_로그인.py`
  - `KREAM_헤더_필수값_가져오기` (NUXT config → apiVersion/buildVersion/webRequestSecret/webDid)
  - `KREAM_login_이메일_request` (POST /api/auth/login)
- **Workflow**: `.github/workflows/phase_a_login.yml` (workflow_dispatch 수동 트리거)
- **Secrets 필요**: `KREAM_EMAIL`, `KREAM_PW` (사용자가 GitHub UI에서 직접 등록)
- **판정 기준**
  - ✅ status 200 + access_token 수신 → Phase B 진행
  - ❌ 403/429/차단 → Azure IP 차단 의심, 실행환경 대안 필요
  - ❌ 캡챠/2차인증 응답 → requests 방식 불가, 셀레니움 검토

- 실행일: _미실행_
- 결과: -

### Phase B~D: 미착수
