# test/

본 코드(프로덕션 로직)와 **명확히 구분된 실험·PoC 공간**.

- 여기 들어가는 코드는 일회성 검증용
- 여기서 검증된 로직만 추후 본 코드(`마켓/`, `코어/`, `시트/`)로 이식
- 본 코드는 이 폴더를 import 하지 않는다

## 현재 하위

- `phase_a_kream_login/` — KREAM 이메일 로그인 API가 GitHub Actions에서 동작하는지 검증
